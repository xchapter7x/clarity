name: Mark stale issues and pull requests

on:
  schedule:
  - cron: "0 0 * * *"

jobs:
  stale:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/stale@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        stale-issue-message: 'This issue has been inactive for a while and is now considered stale'
        stale-pr-message: 'This PRE has been inactive for a while and is now considered stale'
        stale-issue-label: 'no-issue-activity'
        stale-pr-label: 'no-pr-activity'
